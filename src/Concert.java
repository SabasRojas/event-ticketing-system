/**
 * Concert: Class that holds concert information. Uses super()
 * @author Alan Holguin
 * @author Christian Odom
 * @author Jorge Sandoval
 * @author Sabas Rojas
 */

public class Concert extends Event{
    
    // Default constructor:
    public Concert(){}

    // Constructor that uses super()
    // public Concert(String nameIn, String typeIn, int capacityIn, double costIn) {
    //     super(capacityIn, nameIn, typeIn, nameIn, getVenue(), typeIn, costIn, costIn, costIn, costIn, costIn, costIn, costIn, costIn, costIn, costIn, costIn, costIn, costIn, costIn, costIn, capacityIn, costIn);
    // }

}